interface:
  display_name: "Semantic SRS Review"
  short_description: "Create decks, review cards, and open the dashboard"
  default_prompt: "Use $conduct-semantic-review to start a due-card review session."
dependencies:
  tools:
    - type: "mcp"
      value: "semantic-srs"
      description: "Local deck, review-history, statistics, and FSRS scheduling tools"
policy:
  allow_implicit_invocation: true
